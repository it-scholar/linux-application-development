{{- if and .Values.query.enabled .Values.query.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "weather-station.fullname" . }}-query-data
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "weather-station.query.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.query.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.query.persistence.size }}
  {{- if .Values.query.persistence.storageClass }}
  storageClassName: {{ .Values.query.persistence.storageClass }}
  {{- else if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
{{- end }}
